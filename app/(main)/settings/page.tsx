import { AccountSettings } from "@/components/ui/account-settings";

export default function SettingsPage() {
    return (
        <main className="min-h-screen bg-background text-foreground flex p-0 md:p-0">
            <AccountSettings />
        </main>
    );
}
